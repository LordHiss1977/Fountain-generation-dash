Switches between views of the same data; the active tab is marked with the 3px teal rule.

```jsx
<Tabs tabs={[{value:"resumen",label:"Resumen"},{value:"ordenes",label:"Órdenes"}]} onChange={setView} />
```

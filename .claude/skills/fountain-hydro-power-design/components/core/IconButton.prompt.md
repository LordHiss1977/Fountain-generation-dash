A square icon-only action for toolbars and table rows; `label` is mandatory because there is no visible text.

```jsx
<IconButton icon={<Icon name="refresh-cw" size={18} />} label="Actualizar" />
```

Ghost by default, `primary` for a solid teal square. Boxes are 30/38/46px matching Button heights.

A small all-caps label for categories and counts; 2px corners, tinted background, never pill-shaped.

```jsx
<Badge tone="brand">Semana 24</Badge>
```

For live operational state use `StatusPill` instead — Badge is for classification, not status.

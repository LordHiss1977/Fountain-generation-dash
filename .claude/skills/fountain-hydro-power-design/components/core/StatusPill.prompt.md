Shows the live state of a unit, plant or work order as a coloured dot plus a text label.

```jsx
<StatusPill status="planned" />
```

The dot alone is never sufficient — colour always ships with words. One of the only two places `--radius-pill` is allowed.
